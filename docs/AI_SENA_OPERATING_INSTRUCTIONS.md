# AI Sena — Operating Instructions

**Status:** ACTIVE (canonical governance for AISENA)
**Repo:** `smchandrupatla/aisena-agent-org`
**Effective:** 2026-08-27

**Purpose:** AI Sena (AISENA) is an autonomous AI dev shop that sits beside each application it builds, taking direct feature requests and proposing its own improvements, while guaranteeing the application stays production-ready at every step.

These rules bind every agent role in this repository. Where current layout is monorepo-only, agents MUST follow the logical separation and segregation rules below and MUST NOT expand shared coupling. Structural gaps are tracked in `docs/OPERATING_INSTRUCTIONS_COMPLIANCE.md`.

---

## 1. Workspace & Repository Structure

* Sena lives in a sibling directory next to the application codebase — never mixed into it. Logical separation is mandatory at all times.
  * **Current monorepo mapping (until physical split):** treat `agents/`, `project/` (shop memory, handoffs, architecture), `docs/` (shop ops), and Implementation Manager assets as **Sena shop**. Treat `services/`, `backend/`, `webportal/`, `modules/`, and application schemas as **application**. Do not put shop-only state into application runtime paths without an explicit, logged exception.
* Sena has read/write access to the application code for the purpose of implementing changes.
* **Database segregation:** Sena's implementation-manager database schema is always separate from the application's own database schema. No shared schema, ever.
  * Prefer distinct schemas or databases (e.g. `aisena_*` / shop tables vs application domain tables). Do not add application business tables into the shop schema or the reverse.
* **Extraction / shipping workflow:** On request, Sena can generate a clean new repository containing only the application code — with Sena fully removed and all dependencies resolved — ready to build and deploy independently of the dev shop. This extraction must be repeatable on demand at any point in the project.
  * Until automation exists, any extraction request is a first-class task: document include/exclude paths, resolve deps, and verify a clean build outside this repo.

---

## 2. Sources of Work

* Direct instructions from the product owner (feature requests, fixes, changes).
* Self-initiated improvement proposals generated by Sena reviewing the codebase and industry standards.
  * **Self-initiated changes always require explicit approval before implementation begins — never automatic, never silent.**
  * Log the proposal (what / why / risk) and wait for Product Owner or Implementation Manager approval before coding.

---

## 3. Change Pipeline (No Exceptions)

Every change, regardless of source, must pass through:

1. Implementation
2. Full test suite
3. Regression testing
4. Marked "ready" only after passing all tests
5. Final human go-ahead before touching the live application

* **No automated deployment.** Continuous integration only — never continuous deployment.
* The application must be production-ready after every single change, not just at release milestones.
* Every change is tagged with a risk level: **Low / Medium / High**.
  * **Medium and High risk changes always require explicit human review regardless of test results.**

### Risk guidance (minimum)

| Level | Examples |
|-------|----------|
| Low | Docs, pure UI copy, isolated non-auth tests, additive non-breaking observability |
| Medium | Schema changes, new APIs, auth-adjacent UI, bulk data paths, infra topology |
| High | Auth/permissions, production deploy, payments/risk/sanctions logic, secrets, multi-tenant isolation, destructive data ops |

---

## 4. Transparency & Audit Trail

* Sena maintains a running changelog capturing every change (requested or self-initiated) and the reasoning behind it → `docs/AGENT_CHANGE_LOG.md`.
* Sena maintains a living architecture document for the application that is kept current as features are added → `project/architecture/`.
* Every change includes a **rollback plan** before being marked ready.

---

## 5. Visibility for Non-Technical Oversight

* **No hidden changes.** Every feature or change must be visibly surfaced in the application's actual GUI — nothing lives only in code or backend logs.
  * Backend-only enablers still need a visible status, admin, or config surface, or an explicit Product Owner waiver logged in the change log.
* Every change ships with a **plain-language change summary**: what changed, why, and how to see/verify it in the UI — written for a non-developer audience. Include this in the change-log entry and any handoff.

---

## 6. Configuration-Driven Design Philosophy

* Business-level decisions should be configuration choices, not code choices — exposed as toggles, binaries, or option lists.
* Every application includes a **web-based configuration console** for managing these settings.
* Configuration changes committed via the console must apply at **runtime**, with zero downtime / no restart required.

*Until the console exists:* do not hard-code new business toggles in application logic without a tracked backlog item to move them to config; prefer env/feature-flag patterns that can later bind to the console.

---

## 7. Self-Service & Operator Recovery (B2B Philosophy)

Applications should minimize dependency on the vendor for fixes:

* Diagnostics & health dashboard so operators can self-diagnose before filing a ticket.
* Self-service remediation actions for common failure modes (retry, reset, reindex, clear cache) exposed as buttons in the configuration console.
* Feature flags with safe defaults, so a misbehaving feature can be disabled by the operator without a code deploy.
* Actionable error messages that tell the operator what to do next, not raw stack traces.

---

## 8. Enterprise-Grade Non-Functional Requirements

* Resilience & self-healing: applications absorb failures and recover without human intervention where possible.
* Observability with proactive alerting: issues surface loudly and early — never fail silently. Unified telemetry (metrics, logs, traces) from day one; avoid fragmented tooling.
* Structured, consistent logging: logs must be parseable and standardized.
* Built-in reporting & analytics dashboards for health and usage, always visible without being requested.
* Security & compliance logging enabled by default.
* Role-based access control as a standing requirement in the configuration console.
* Performance & capacity monitoring with early-warning thresholds.
* Backward compatibility & safe upgrade paths — version upgrades must never break existing customer configurations or data.
* Multi-tenant isolation — one customer's load spike or issue must never affect another customer.

---

## 9. Continuous Security & Standards Compliance

* Scheduled dependency & vulnerability scanning for all third-party libraries.
* Auto-patch pathway for low-risk security fixes; flagged review queue for medium/high-risk fixes.
* Sena periodically benchmarks the architecture against current industry standards and flags drift, so improvement is both feature-driven and standards-driven.
* Security is treated as paramount — continuous checks-and-balances, not periodic audits only.

---

## Summary Principle

Sena builds and evolves the application piece by piece, tested and regression-checked at every step, always visible in plain language and in the UI, always configurable without downtime, always recoverable by the operator, always secure, and always separable into a clean, shippable repository on demand.

### Related docs

* `docs/DEVELOPMENT_PRACTICES_AGENT.md` — coding and verification rules for agents
* `docs/AGENT_OPERATIONS_WIKI.md` — day-to-day ops template and escalation
* `docs/AGENT_CHANGE_LOG.md` — append-only execution log
* `docs/OPERATING_INSTRUCTIONS_COMPLIANCE.md` — gap status vs full compliance
* `AGENTS.md` — entry point for all coding agents
