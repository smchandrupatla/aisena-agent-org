# Dev-only placeholder secrets. Replace values (or generate via `kubectl create secret`
# / Vault / Sealed Secrets / External Secrets Operator) before using outside local dev.
apiVersion: v1
kind: Secret
metadata:
  name: aisena-secrets
  namespace: aisena
type: Opaque
stringData:
  POSTGRES_PASSWORD: "aisena_pw"
  GRAFANA_ADMIN_PASSWORD: "admin"
  VAULT_DEV_ROOT_TOKEN_ID: "root"
  REDMINE_DB_PASSWORD: "aisena_pw"
