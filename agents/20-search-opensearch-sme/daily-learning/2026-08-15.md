# Daily Learning Report — Agent 20: Search & OpenSearch SME
Date: 2026-08-15

---

FINDING: OpenSearch 3.8 (released August 2026) enables Base64-encoded vector ingestion for `knn_vector` fields, delivering up to 4.16x higher bulk ingestion throughput and 83% lower median latency with no mapping changes required.

WHY_IT_MATTERS: AISENA's Stage 0 proof indexes screening results into OpenSearch (`aisena-stage0-screening-results`). As the system scales toward full transaction volumes, vector ingestion will become a bottleneck for AI-powered similarity search and RAG over sanctions/fraud case data. Upgrading to OpenSearch 3.8 and switching vector payloads from JSON arrays to Base64 encoding is a low-risk, high-ROI optimization — a 768-dim float vector shrinks from ~16 KB to ~4 KB, cutting network overhead by 74%. The gRPC-based streaming ML predictions also directly benefit the agent runtime's inference latency.

EVIDENCE:
- https://opensearch.org/blog/whats-new-in-opensearch-3-8/ (August 2026)
- https://docs.opensearch.org/latest/vector-search/ingesting-data/index/
- https://github.com/opensearch-project/OpenSearch/releases

RECOMMENDED_ACTION: Add a backlog story to pin the OpenSearch Docker image in `infra/docker-compose.yml` to `opensearch:3.8` and update the `services/detection/consume.py` indexing call to send `knn_vector` payloads as Base64 strings. Estimate: 1–2 hours, zero breaking changes to existing mappings or queries. Validates in Stage 0 smoke test (`scripts/run-stage0-smoke.sh`).
