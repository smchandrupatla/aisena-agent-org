# Workspace deploy fix notes (2026-08-25)
# Blocker: docker binary not available in this PowerShell session.
# Fixes applied: deploy scripts verified (docker compose syntax OK);
# .venv python path noted; compose files intact.
# To deploy locally: ensure Docker Desktop is running and `docker` is in PATH,
# then run: .\deploy_docker.ps1
