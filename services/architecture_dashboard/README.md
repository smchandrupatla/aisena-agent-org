# Architecture Dashboard

A lightweight local application that serves an interactive architecture diagram for the Hybrid Sanctions & Fraud Monitoring System.

Run the app with:

```bash
python3 services/architecture_dashboard/app.py
```

Then open `http://localhost:8080`.

The HTML page supports interactive component selection and displays purpose, key logic, and typical technology choices for each architecture node.
